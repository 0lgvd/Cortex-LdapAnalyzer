echo "# Cortex LdapAnalyzer" > README.md
echo "Analyzer LDAP pour Cortex permettant de récupérer les machines récemment modifiées." >> README.md
git add README.md
git commit -m "Ajout du README"
git push origin main
